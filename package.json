{
  "name": "salesloft-pricing-signal-extractor",
  "version": "1.0.0",
  "private": true,
  "description": "Extract structured pricing insights from Salesloft call transcripts via OpenAI",
  "scripts": {
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "dev": "concurrently -k \"npm run dev:server\" \"npm run dev:client:wait\"",
    "dev:client:wait": "wait-on -t 60000 http://localhost:3001/insights && npm run dev:client",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "test:integration": "cd server && node scripts/run-mock-analysis.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "wait-on": "^8.0.1"
  }
}
